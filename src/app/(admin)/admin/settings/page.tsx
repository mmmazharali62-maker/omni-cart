// Settings.
// Store settings, roles/permissions, notification config.
// TODO: replace this placeholder with real data + UI.

export default function AdminSettingsPage() {
  return (
    <section className="glass p-8 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Settings</h1>
      <p className="text-white/60 mt-2">Store settings, roles/permissions, notification config.</p>
    </section>
  );
}
