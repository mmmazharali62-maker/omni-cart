// Orders.
// All orders with status, retry, cancel, refund actions (spec section 7/32).
// TODO: replace this placeholder with real data + UI.

export default function AdminOrdersPage() {
  return (
    <section className="glass p-8 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Orders</h1>
      <p className="text-white/60 mt-2">All orders with status, retry, cancel, refund actions (spec section 7/32).</p>
    </section>
  );
}
