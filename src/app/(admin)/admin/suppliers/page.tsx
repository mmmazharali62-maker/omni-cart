// Supplier Dashboard.
// CJ + AliExpress connection status, credential config, mapping, sync status, failed syncs, API errors (spec section 16).
// TODO: replace this placeholder with real data + UI.

export default function AdminSupplierDashboardPage() {
  return (
    <section className="glass p-8 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Supplier Dashboard</h1>
      <p className="text-white/60 mt-2">CJ + AliExpress connection status, credential config, mapping, sync status, failed syncs, API errors (spec section 16).</p>
    </section>
  );
}
