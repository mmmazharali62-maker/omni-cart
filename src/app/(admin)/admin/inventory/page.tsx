// Inventory.
// Stock levels, low-stock alerts, manual overrides.
// TODO: replace this placeholder with real data + UI.

export default function AdminInventoryPage() {
  return (
    <section className="glass p-8 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Inventory</h1>
      <p className="text-white/60 mt-2">Stock levels, low-stock alerts, manual overrides.</p>
    </section>
  );
}
