// Admin Dashboard.
// Revenue, orders, customers, products, profit/margin, refunds, failed orders, supplier status, inventory alerts + charts (spec section 14).
// TODO: replace this placeholder with real data + UI.

export default function AdminAdminDashboardPage() {
  return (
    <section className="glass p-8 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Admin Dashboard</h1>
      <p className="text-white/60 mt-2">Revenue, orders, customers, products, profit/margin, refunds, failed orders, supplier status, inventory alerts + charts (spec section 14).</p>
    </section>
  );
}
