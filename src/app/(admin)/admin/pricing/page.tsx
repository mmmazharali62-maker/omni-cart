// Pricing Rules.
// Configure PricingRule entries consumed by src/lib/pricing-engine.ts (spec section 10).
// TODO: replace this placeholder with real data + UI.

export default function AdminPricingRulesPage() {
  return (
    <section className="glass p-8 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Pricing Rules</h1>
      <p className="text-white/60 mt-2">Configure PricingRule entries consumed by src/lib/pricing-engine.ts (spec section 10).</p>
    </section>
  );
}
