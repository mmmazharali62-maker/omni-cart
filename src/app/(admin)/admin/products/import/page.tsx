// Import Product.
// One-click import: paste a CJ Dropshipping / AliExpress product URL or ID -> POST /api/suppliers/{provider}/import.
// TODO: replace this placeholder with real data + UI.

export default function AdminImportProductPage() {
  return (
    <section className="glass p-8 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Import Product</h1>
      <p className="text-white/60 mt-2">One-click import: paste a CJ Dropshipping / AliExpress product URL or ID -> POST /api/suppliers/{provider}/import.</p>
    </section>
  );
}
