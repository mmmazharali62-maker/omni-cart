// Wishlist page.
// Signed-in user's saved WishlistItem records.
// TODO: replace this placeholder with the real UI + data fetching.

export default function WishlistPage() {
  return (
    <section className="mx-4 mt-12 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Wishlist</h1>
      <p className="text-white/60 mt-2">Signed-in user's saved WishlistItem records.</p>
    </section>
  );
}
