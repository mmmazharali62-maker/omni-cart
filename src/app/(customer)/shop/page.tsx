// Shop page.
// All products, with filters (price, rating, availability, brand, category, shipping, discount) and sort (spec section 2).
// TODO: replace this placeholder with the real UI + data fetching.

export default function ShopPage() {
  return (
    <section className="mx-4 mt-12 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Shop</h1>
      <p className="text-white/60 mt-2">All products, with filters (price, rating, availability, brand, category, shipping, discount) and sort (spec section 2).</p>
    </section>
  );
}
