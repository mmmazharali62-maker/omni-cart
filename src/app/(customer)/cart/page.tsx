// Cart page.
// Smart Cart (spec section 4): quantities, variants, subtotal, discounts, shipping, tax, coupon, save-for-later.
// TODO: replace this placeholder with the real UI + data fetching.

export default function CartPage() {
  return (
    <section className="mx-4 mt-12 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Cart</h1>
      <p className="text-white/60 mt-2">Smart Cart (spec section 4): quantities, variants, subtotal, discounts, shipping, tax, coupon, save-for-later.</p>
    </section>
  );
}
