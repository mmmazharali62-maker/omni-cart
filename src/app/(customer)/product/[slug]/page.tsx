// Product page.
// Full product detail page (spec section 3): gallery, variants, price, description, specs, reviews, related products.
// TODO: replace this placeholder with the real UI + data fetching.

export default function ProductPage() {
  return (
    <section className="mx-4 mt-12 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Product</h1>
      <p className="text-white/60 mt-2">Full product detail page (spec section 3): gallery, variants, price, description, specs, reviews, related products.</p>
    </section>
  );
}
