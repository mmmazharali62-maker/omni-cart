// Search page.
// Search bar, autocomplete, filters, and later AI natural-language search (spec section 2).
// TODO: replace this placeholder with the real UI + data fetching.

export default function SearchPage() {
  return (
    <section className="mx-4 mt-12 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Search</h1>
      <p className="text-white/60 mt-2">Search bar, autocomplete, filters, and later AI natural-language search (spec section 2).</p>
    </section>
  );
}
