// Checkout page.
// Guest/account checkout, addresses, shipping method, order summary, payment (spec section 5).
// TODO: replace this placeholder with the real UI + data fetching.

export default function CheckoutPage() {
  return (
    <section className="mx-4 mt-12 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Checkout</h1>
      <p className="text-white/60 mt-2">Guest/account checkout, addresses, shipping method, order summary, payment (spec section 5).</p>
    </section>
  );
}
