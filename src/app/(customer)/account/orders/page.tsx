// My Orders page.
// All orders for the signed-in customer.
// TODO: replace this placeholder with the real UI + data fetching.

export default function MyOrdersPage() {
  return (
    <section className="mx-4 mt-12 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">My Orders</h1>
      <p className="text-white/60 mt-2">All orders for the signed-in customer.</p>
    </section>
  );
}
