// Notifications page.
// Order confirmation, shipment, delivery, and account alerts.
// TODO: replace this placeholder with the real UI + data fetching.

export default function NotificationsPage() {
  return (
    <section className="mx-4 mt-12 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Notifications</h1>
      <p className="text-white/60 mt-2">Order confirmation, shipment, delivery, and account alerts.</p>
    </section>
  );
}
