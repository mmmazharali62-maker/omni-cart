// Saved Products page.
// Alias of /wishlist scoped to the account section.
// TODO: replace this placeholder with the real UI + data fetching.

export default function SavedProductsPage() {
  return (
    <section className="mx-4 mt-12 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Saved Products</h1>
      <p className="text-white/60 mt-2">Alias of /wishlist scoped to the account section.</p>
    </section>
  );
}
