// Category page.
// Products within a single category, driven by the Category/Product Prisma models.
// TODO: replace this placeholder with the real UI + data fetching.

export default function CategoryPage() {
  return (
    <section className="mx-4 mt-12 min-h-[40vh]">
      <h1 className="text-2xl font-semibold">Category</h1>
      <p className="text-white/60 mt-2">Products within a single category, driven by the Category/Product Prisma models.</p>
    </section>
  );
}
